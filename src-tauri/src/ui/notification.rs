pub struct NotificationController;

impl NotificationController {
    pub fn new() -> Self {
        Self
    }

    pub fn show(&self, _message: &str) {
        // Tauri window hook placeholder: frameless, transparent, frosted effect.
    }
}
